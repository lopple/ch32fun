# b003fast minichlink release assets

This branch adds a GitHub Actions release workflow for b003fast minichlink release candidates.

## Release workflow

Workflow file:

```text
.github/workflows/minichlink-release.yml
```

Release tags use this pattern:

```text
b003fast-minichlink-rc.*
```

Pushing a matching tag builds all release archives on GitHub and publishes a GitHub prerelease.
Manual `workflow_dispatch` runs can build the same artifacts without publishing a release.

## Targets

| Asset | Runner | Notes |
|---|---|---|
| `minichlink-windows-x86_64-<tag>.zip` | `ubuntu-24.04` | Cross-compiled with MinGW and packaged with `bin/minichlink.exe` plus `bin/libusb-1.0.dll`. |
| `minichlink-linux-x86_64-<tag>.tar.gz` | `ubuntu-24.04` | Built natively and packaged with `bin/minichlink`, `lib/minichlink.so`, and udev rules. |
| `minichlink-macos-x86_64-<tag>.tar.gz` | `macos-15-intel` | Built on an Intel macOS runner. |
| `minichlink-macos-arm64-<tag>.tar.gz` | `macos-14` | Built on an Apple Silicon runner so the binary does not require Rosetta. |

Each archive has one top-level directory and stores the executable under `bin/`.
The workflow also publishes `SHA256SUMS.txt` for package-index work.

## Local notes

The workflow is intentionally separate from the existing push/pull-request minichlink CI.
Release archives are generated by GitHub Actions and are not committed to the repository.

## Published release candidate

| Item | Value |
|---|---|
| Tag | `b003fast-minichlink-rc.1` |
| Release | https://github.com/lopple/ch32fun/releases/tag/b003fast-minichlink-rc.1 |
| Workflow run | https://github.com/lopple/ch32fun/actions/runs/28630763116 |
| Target commit | `f06b02ed39478f9a03c36c63caad1ac5cc78239f` |
| Result | All build jobs and the prerelease publish job passed. |

Published assets:

| Asset | Size | SHA-256 |
|---|---:|---|
| `minichlink-windows-x86_64-b003fast-minichlink-rc.1.zip` | `184428` | `cdad6ace3351287376f1f4f0557dc36cf69819005a4a113e2585befc44923f6d` |
| `minichlink-linux-x86_64-b003fast-minichlink-rc.1.tar.gz` | `547586` | `eca04731541a8c3f8e3680bcc46c3d2af41d65afb3e784c07e488983680f7ae7` |
| `minichlink-macos-x86_64-b003fast-minichlink-rc.1.tar.gz` | `261606` | `8dea75b5d0d9f722a9a88fa6434f4a530e1b88809975d5d32ca2cdd9d299d662` |
| `minichlink-macos-arm64-b003fast-minichlink-rc.1.tar.gz` | `276664` | `91df05682223ef61d332174d1e9806173239c7ac07eae97cf747d097544130f7` |
| `SHA256SUMS.txt` | `486` | `6be3bd71f0d551d35508052ad92f33c60b367a600de8dca0a28e973f198f8c18` |

Architecture checks from the workflow log:

```text
minichlink-windows-x86_64: PE32+ executable (console) x86-64
minichlink-linux-x86_64: ELF 64-bit LSB pie executable, x86-64
minichlink-macos-x86_64: Non-fat file: minichlink/minichlink is architecture: x86_64
minichlink-macos-arm64: Non-fat file: minichlink/minichlink is architecture: arm64
```

Downloaded asset verification confirmed that every archive has one top-level root and places the executable under `bin/`.


